using Gtk 4.0;

Box {
    orientation: horizontal;
    spacing: 6;
    margin-top: 4;
    margin-bottom: 4;
    margin-start: 8;
    margin-end: 8;

    Label note_name_label {
        halign: start;
        hexpand: true;
        ellipsize: end;
    }
}
